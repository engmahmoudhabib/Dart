/// extension methods is a way to add functonality to existing libraries
///

extension IntegerIntegrated on int {
  int get luckyNumber => 12;
}

/// use it like that
extension  on String {
  bool countStringDots() => this.contains('.');
}
